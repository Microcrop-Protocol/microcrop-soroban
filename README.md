# microcrop-soroban

MicroCrop parametric-insurance contracts on **Stellar (Soroban)** — a Rust/WASM port of the EVM contracts (PolicyManager, Treasury, PayoutReceiver, PolicyNFT). Preparation only; not migrating yet.
